# Comma-separated browser origins allowed to call the local agent bridge.
STORYSTAGE_ALLOWED_ORIGINS=http://127.0.0.1:5173,http://localhost:5173

# Optional; defaults to 4175.
# STORYSTAGE_BRIDGE_PORT=4175